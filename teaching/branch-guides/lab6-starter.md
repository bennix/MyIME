# Lab 6 starter — Explainable scoring

Populate every `ScoreBreakdown` term, keep units explicit, and expose a stable sort
key. Run frequency/user/context/fuzzy ablations on a fixed development set. The
sealed test set must not be used for per-example tuning.
