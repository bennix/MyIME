# Lab 8 starter — Local learning

Implement one user unigram/bigram/recency signal behind `LearningStore`. Writes must
be ordered off the key-event path. Test restart persistence, clear, read-only DB,
corruption, and concurrent selections; core typing must degrade safely.
