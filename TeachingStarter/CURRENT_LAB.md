# Lab 12 starter — Clean-Mac release acceptance

Complete every `ReleaseCheck` using a Mac with no source tree, developer certificate,
or old MyIME copy. Install only the public notarized DMG, test Notes, kill/recover the
IME via ABC→MyIME, reboot, retest, and document uninstall data retention.
