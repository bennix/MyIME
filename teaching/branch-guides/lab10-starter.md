# Lab 10 starter — Diagnostic bundle

Produce a local, expiring diagnostic directory using `DiagnosticSnapshot`. Include
version, path, signature summary, TIS state, OS build, architecture, schema and the
last 200 redacted log lines. Never include raw input or candidate/commit text.
