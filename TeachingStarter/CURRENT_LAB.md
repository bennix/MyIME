# Lab 1 starter — Echo IME

Implement the smallest IMKServer/controller adapter. Handle printable ASCII only;
return `true` only for events the IME actually owns. No database, candidate window,
installer daemon, or production MyIME source may be copied. Evidence: Notes input,
callback log, and one unhandled-modifier case.
