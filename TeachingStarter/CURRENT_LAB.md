# Lab 2 starter — Composition state

Implement `CompositionState.reduce` as a table-tested pure state machine. Cover
append, empty/non-empty backspace, commit, cancel, and deactivation. AppKit code may
only translate `Effect` values to marked/inserted text.
